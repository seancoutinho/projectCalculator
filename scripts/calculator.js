//this function will add two numbers
const add = (num1, num2) =>  {
    return num1 + num2;
}

//this function will subract the second param from the first one
const subtract = (num1, num2) => {
    return num1-num2;
}

//this function will divide num1 by num2
const multiply = (num1, num2) => {
    return num1*num2;
}

//this function will devide num1 by num2
const divide = (num1, num2) => {
    while (num2 != 0) {
        return num1/num2;
    } return 'Not a number!'
}

